{
  "name": "http-observer",
  "version": "1.0.0",
  "description": "simple http proxy server - observing http requests, responsens and modyfying it",
  "main": "lib/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node ./lib/main.js"
  },
  "keywords": [
    "http",
    "proxy",
    "server",
    "mitm"
  ],
  "author": "totalzero",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "@types/node": "^20.3.1",
    "save-dev": "^0.0.1-security"
  }
}
